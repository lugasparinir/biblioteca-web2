<?php 
?>

<div class="container">
    <?php if (isset($user) && $user): ?> 
        <a href="<?php echo BASE_URL . 'agregarlibro'; ?>" class="button-add">Agregar Nuevo Libro</a>
    <?php endif; ?>

    <h1>Catálogo de Libros</h1>

    <?php if (!empty($libros)): ?>
        <ul class="listalibros">
        <?php foreach ($libros as $libro): ?>
            <li class="libro">
                <a href="<?php echo BASE_URL . 'mostrarlibro/' . $libro->id; ?>">
                    <h2><?php echo htmlspecialchars($libro->titulo); ?></h2> 
                </a>
                
                <p>Género: <a href="<?php echo BASE_URL . 'librosporgenero/' . $libro->id_genero; ?>">
                    <?php echo htmlspecialchars($libro->nombre_genero); ?> 
                </a></p>
            </li>
        <?php endforeach; ?>
        </ul>
    <?php else: ?>
        <p>No se encontraron libros para mostrar.</p>
    <?php endif; ?>

</div>
